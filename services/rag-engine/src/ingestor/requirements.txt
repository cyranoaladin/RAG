fastapi==0.115.0
uvicorn[standard]==0.30.6

langchain==0.2.14
langchain-community==0.2.12
langchain-text-splitters==0.2.2
langchain-google-community[drive]==1.0.7

chromadb-client==1.1.1
ollama==0.3.0

# Multimodal extras pull in large GPU stacks; keep default lean.
unstructured==0.14.5
# `pypdf` fixe le TEXTE extrait, donc les empreintes de chunks. Une contrainte
# ouverte ici casse la reproductibilité du corpus scellé : `chunk_id` vaut
# sha256(content_sha:index:sha256(texte)), et deux versions de pypdf n'extraient
# pas le même texte. Constaté le 28/08/2026 — 4.2.0 rendait « enseigneme nt » là
# où 6.14.2 rend « enseignement ». Le rejeu échouait sur « Chunk ID mismatch ».
# 6.14.2 reproduit les 44 empreintes du document témoin ; c'est la version du
# scellement, et elle se fige.
pypdf==6.14.2
prometheus-client==0.20.0
python-docx==1.1.2
beautifulsoup4==4.12.3
requests==2.32.3
python-multipart>=0.0.9

# OCR & image processing for multimodal ingestion
pytesseract==0.3.13
pdfplumber==0.11.4
Pillow==10.4.0

# Google Drive integration
google-auth==2.34.0
google-api-python-client==2.147.0

# Retrieval v2 (pgvector, FE-01)
psycopg==3.2.1
psycopg-binary==3.2.1
psycopg-pool==3.2.1
