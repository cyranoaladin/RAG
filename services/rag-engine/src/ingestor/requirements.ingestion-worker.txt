# Runtime worker d'ingestion gouvernée uniquement (LOT44e/LOT44f).
#
# Volontairement minimal : audité contre les imports réels de
# ingestion_control/, ingestion_agents/, ingestion_profiles/,
# ingestion_worker/ et ssrf_guard.py (aucun langchain, aucun parseur PDF/
# DOCX/HTML ni client Google Drive. L'image porte désormais aussi Worker B,
# qui publie dans pgvector avec l'artefact E5 local attesté. N'ajouter une
# dépendance ici que lorsqu'un import réel l'exige.

--extra-index-url https://download.pytorch.org/whl/cpu

psycopg==3.2.1
psycopg-binary==3.2.1
psycopg-pool==3.2.1
httpx==0.27.2
PyYAML==6.0.3
pydantic==2.13.4
# `pypdf` fixe le TEXTE extrait, donc les empreintes de chunks. Une contrainte
# ouverte ici casse la reproductibilité du corpus scellé : `chunk_id` vaut
# sha256(content_sha:index:sha256(texte)), et deux versions de pypdf n'extraient
# pas le même texte. Constaté le 28/08/2026 — 4.2.0 rendait « enseigneme nt » là
# où 6.14.2 rend « enseignement ». Le rejeu échouait sur « Chunk ID mismatch ».
# 6.14.2 reproduit les 44 empreintes du document témoin ; c'est la version du
# scellement, et elle se fige.
pypdf==6.14.2
numpy==1.26.4
torch==2.4.1+cpu
transformers==4.44.2
sentence-transformers==3.0.1
