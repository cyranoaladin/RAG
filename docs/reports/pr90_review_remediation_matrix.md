# Matrice de remédiation — revue PR#90 (Codex, Cubic, GitGuardian)

- **PR** : https://github.com/cyranoaladin/RAG/pull/90
- **HEAD au moment de la revue** : `9306f1cb4f92037c09a2edb63b78665be0afcc79`
- **HEAD remédié** : voir `docs/reports/lot_44f_reconciliation_worker_isole.md` (mis à jour) pour le SHA final exact — toute approbation de l'ancien SHA est caduque, conformément au mandat de revue.
- **Portée** : 40 signalements Cubic, 5 signalements Codex (dont recoupements avec Cubic), 5 alertes GitGuardian.
- **Règle appliquée** : aucun signalement P1/P2 n'a été corrigé mécaniquement sans analyse — chaque ligne ci-dessous documente le diagnostic réel avant toute correction.

## Légende Statut

- `CORRIGÉ` — root cause identifiée, correctif appliqué, test de non-régression réel ajouté et vérifié vert.
- `FAUX POSITIF CONFIRMÉ` — reproduit/vérifié empiriquement invalide ; preuve documentée.
- `HORS PÉRIMÈTRE (documenté)` — valide en principe, mais implémentation complète hors périmètre d'une passe de remédiation de revue (justifié ligne par ligne).

| ID | Source | Gravité | Fichier | Problème | Diagnostic | Décision | Correction | Tests | Fil GitHub | Statut |
|---|---|---|---|---|---|---|---|---|---|---|
| C1 | Cubic | P1 | infra/Dockerfile.ingestion-worker:11 | Le build échouerait car `.dockerignore` exclut les fichiers copiés | Reproduit deux fois : `docker build` direct et `docker compose build` réel, avec `--no-cache`, depuis un contexte propre — le build **réussit** dans les deux cas. Cause : moby's dockerignore re-inclut les fichiers descendants une fois le répertoire parent explicitement un-ignoré (`!services/rag-engine/src/ingestor/`) — comportement documenté, pas un bug. | Faux positif confirmé, mais fragile pour un futur mainteneur | Ajout d'entrées d'allowlist explicites dans `.dockerignore` pour `ingestion_control/`, `ingestion_agents/`, `ingestion_profiles/`, `ingestion_worker/`, `ssrf_guard.py`, `requirements.ingestion-worker.txt`, `Dockerfile.ingestion-worker` — clarté, aucun changement de comportement | Build réel revérifié après ajout (`docker build --no-cache`, succès) ; build `Dockerfile.ingestor-v2` revérifié aussi (partage le même `.dockerignore`) | à répondre | **FAUX POSITIF CONFIRMÉ + durci** |
| X3/C3 | Codex+Cubic | P1 | ingestion_agents/scout.py:56 | `domain` déclaré par l'appelant n'est jamais comparé à l'hôte réel de `source_url` | Confirmé : `discover_candidate_core` ne validait que la chaîne `domain` contre `allowed_domains`, jamais l'URL réelle | Corrigé | `_hostname_of()` dérive l'hôte depuis `source_url`/`canonical_url` (`urlparse`, normalisé casse+point final) ; `domain` doit correspondre exactement à l'hôte de `source_url` ; hôte de `canonical_url` doit aussi être allowlisté | `tests/test_lot44d_scout.py` : 3 nouveaux tests (mismatch domain/URL rejeté, canonical_url hors allowlist rejeté, normalisation casse/point) | à répondre | CORRIGÉ |
| C4 | Cubic | P1 | infra/docker-compose.v2.yml:165 | `migrator-ingestion-control`/`ingestion-worker` démarrent avec `make v2-up` normal | Confirmé : les deux services vivaient dans `docker-compose.v2.yml`, chargés par la commande normale | Corrigé | Déplacés dans un fichier séparé strictement opt-in `docker-compose.ingestion.yml` ; `docker-compose.v2.yml` restauré à l'état exact d'`origin/main` (`git diff origin/main` : zéro ligne supprimée) ; nouvelle cible `make v2-ingestion-up` | `tests/test_prod_compose_config_mount.py` : assertion stricte restaurée + nouveaux tests (fichier séparé, aucune variable d'ingestion dans le fichier normal) ; `docker compose config` revérifié dans les deux configurations | à répondre | CORRIGÉ |
| C5 | Cubic | P1 | ingestion_agents/fetcher.py:96 | Statut HTTP jamais vérifié — un 404/500 est traité comme un artefact valide | Confirmé : aucun contrôle de `response.status_code` | Corrigé | `FetchHTTPError` (avec `status_code`/`retryable`) levée avant hash/store/transition si `status_code >= 400` ; distinction 4xx non-retryable vs 429/5xx retryable | `tests/test_lot44d_fetcher.py` : 9 nouveaux tests (404/401/403/429/500/502/503, redirection finissant en erreur, non-régression succès) | à répondre | CORRIGÉ |
| C6 | Cubic | P1 | ingestion_control/jobs.py:240 | `complete_job`/`record_job_retry` n'exigent pas `lease_expires_at > now()` | Confirmé : la garde ne vérifiait que `lease_token`+`status`, pas l'expiration | Corrigé | `AND lease_expires_at > now()` ajouté aux deux gardes | `tests/integration/test_lot44e_jobs_control.py` : 2 nouveaux tests (bail expiré sans reprise, sur les deux fonctions) | à répondre | CORRIGÉ |
| C7 | Cubic | P1 | infra/scripts/provision_ingestion_control_roles.sh:44 | Mots de passe visibles dans les arguments du processus psql | Confirmé : passés via `-v migrator_password=...` | Corrigé | Remplacé par `\getenv` (même motif que `provision_runtime_roles.sh`) — jamais sur la ligne de commande | `tests/integration/test_lot44f_role_provisioning_hardening.py::test_password_visible_to_a_process_listing_is_never_the_real_secret` — surveillance `ps -eo args` réelle pendant l'exécution, confirme l'absence des deux mots de passe | à répondre | CORRIGÉ |
| C8/C13 | Cubic (doublon, 2 ancrages) | P1 | ingestion_control/retry.py:95 / claim.py:135 | `record_retry` (resources) ne vérifie aucun `lease_token` | Confirmé : fonction sans paramètre de bail du tout, matchait sur `resource_id` seul. C13 pointe la même cause racine (diff-context voisin dans claim.py, pas une fonction distincte — `claim.py` ne contient aucune fonction "record_retry") | Corrigé | `lease_token` requis + `lease_expires_at > now()` ; nouvelle `ResourceLeaseConflictError` (miroir de `JobLeaseConflictError`) | Fonction sans appelant réel dans ce dépôt (confirmé par recherche) — signature changée librement, sans régression possible | à répondre (C13 : répondre en renvoyant vers C8) | CORRIGÉ |
| C9 | Cubic | P1 | rollbacks/002...down.sql:1 | Fenêtre de course check-puis-drop | Confirmé : `EXISTS` puis `DROP` sans verrou explicite | Corrigé | `LOCK TABLE ... IN ACCESS EXCLUSIVE MODE` ajouté avant la vérification, dans la même transaction | `tests/integration/test_lot44f_migration_rollback_rehearsal.py` rejoué (rollback complet 006→001 puis réapplication) | à répondre | CORRIGÉ |
| C10 | Cubic | P1 | rollbacks/001...down.sql:13 | Même classe que C9 | Confirmé | Corrigé | `LOCK TABLE` ajouté (resources, ingestion_runs) | idem C9 | à répondre | CORRIGÉ |
| C11 | Cubic | P1 | ssrf_guard.py:43 | RFC 6598 (100.64.0.0/10) non bloqué | Confirmé empiriquement : `ipaddress.IPv4Address('100.64.0.1').is_private == False` | Corrigé | `or not ip.is_global` ajouté — **en plus** des vérifications existantes, jamais à leur place (`is_global` vaut `True` pour une adresse multicast, vérifié empiriquement — `is_multicast` reste nécessaire séparément) | `tests/test_ssrf_guard.py` : 2 nouveaux tests (100.64.0.1 rejeté, 224.0.0.1 toujours rejeté malgré `is_global=True`) | à répondre | CORRIGÉ |
| C12 | Cubic | P1 | ingestion_control/jobs.py:138 | `set_job_resource_id` sans garde de bail | Confirmé : `UPDATE` inconditionnel sur `job_id` seul | Corrigé | `lease_token` requis, garde `lease_expires_at > now() AND (resource_id IS NULL OR resource_id = %s)` (idempotent), `JobLeaseConflictError` sinon | `tests/integration/test_lot44e_jobs_control.py::TestSetJobResourceId` : 5 nouveaux tests | à répondre | CORRIGÉ |
| C14 | Cubic | P1 | infra/scripts/provision_ingestion_control_roles.sh:42 | Aucune protection contre `APP_ROLE == MIGRATOR_ROLE` | Confirmé | Corrigé | Vérification explicite avant toute écriture base, échec clair | `tests/integration/test_lot44f_role_provisioning_hardening.py::TestRoleCollisionRejected` — vérifie aussi qu'aucun rôle n'est créé | à répondre | CORRIGÉ |
| C15 | Cubic | P1 | infra/scripts/provision_ingestion_control_roles.sh:66 | Rôle existant : LOGIN/attributs jamais réalignés | Confirmé : seul l'`ALTER ROLE ... PASSWORD` existait pour un rôle déjà présent | Corrigé | `ALTER ROLE` réaligne aussi `LOGIN NOSUPERUSER NOCREATEDB NOCREATEROLE NOINHERIT NOREPLICATION NOBYPASSRLS` | `tests/integration/test_lot44f_role_provisioning_hardening.py::TestPreExistingMisconfiguredRoles` — rôle NOLOGIN réel corrigé, rôle CREATEDB/CREATEROLE réel dépouillé, vérifié sur PostgreSQL réel | à répondre | CORRIGÉ |
| C16 | Cubic | P1 | ingestion_control/jobs.py:83 | `find_active_job_by_dedup_key` + création séparée = fenêtre de course | Confirmé — fonction sans appelant réel dans ce dépôt (même situation que C8) | Corrigé | Nouvelle primitive `find_or_create_job` : `pg_advisory_xact_lock` sur hash du `dedup_key` avant recherche+création, atomique sous concurrence | `tests/integration/test_lot44e_jobs_control.py::TestFindOrCreateJob` — **test concurrent réel** (2 threads, 2 connexions PostgreSQL distinctes, `threading.Barrier`) : converge sur exactement un job | à répondre | CORRIGÉ |
| C17 | Cubic | P1 | packages/contracts/.../ingestion.py:99 | `expected_topics: [""]` contourne le contrôle qualité | Confirmé : `min_length=1` sur la liste ne protège pas le contenu des éléments | Corrigé | `field_validator` rejette toute entrée vide/blanche | `tests/test_lot44c_profile_registry.py` : 2 nouveaux tests | à répondre | CORRIGÉ |
| C18 | Cubic | P1 | rollbacks/003...down.sql:3 | Même classe que C9/C10 | Confirmé | Corrigé | `LOCK TABLE` ajouté (workflow_events) | idem C9 | à répondre | CORRIGÉ |
| C19 | Cubic | P1 | migrations/006...sql:19 | Backfill payload manquant sur upgrade d'une base peuplée | Confirmé — jamais survenu en production (aucun déploiement), mais réel pour une future ré-application | Corrigé (garde explicite plutôt que reconstruction partielle non fiable) | Migration échoue explicitement (`MIGRATION_006_PREEXISTING_ROWS_*`) si la table contient déjà des lignes au moment où la colonne est ajoutée — jamais une reconstruction silencieuse depuis des colonnes typées incomplètes | `tests/integration/test_lot44f_migration_upgrade_paths.py` : ligne réelle pré-existante → migration 006 échoue avec le message attendu ; table vide → succès inchangé | à répondre | CORRIGÉ |
| C20 | Cubic | P1 | migrations/005...sql:30 | `status='claimed'` préexistant bloquerait la migration | Confirmé | Corrigé | `UPDATE ... SET status='running' WHERE status='claimed'` avant l'ajout de la contrainte à 6 valeurs | `tests/integration/test_lot44f_migration_upgrade_paths.py::TestMigration005ClaimedStatusBackfill` — ligne réelle 'claimed' insérée, migration 005 rejouée, statut vérifié réconcilié à 'running' | à répondre | CORRIGÉ |
| X2/C21 | Codex+Cubic | P1 | ingestion_profiles/startup_gate.py:64 / ingestion_worker/runner.py:182 | Le worker recharge le registre depuis le disque à chaque job, contournant le gate de démarrage | Confirmé : `_process_claimed_job` appelait `load_profile_registry(deps.profiles_dir)` à chaque itération | Corrigé | `StartupGateResult.registry` (nouveau champ) porte le registre exact vérifié ; `WorkerDeps.profile_registry` (remplace `profiles_dir`) le garde en mémoire pour toute la durée de vie du worker — plus aucun rechargement disque | `tests/integration/test_lot44e_worker_e2e.py::test_profile_modified_on_disk_after_startup_does_not_affect_running_worker` — profil modifié sur disque après démarrage, job traité avec succès malgré `enabled: False` sur disque (preuve que l'ancien snapshot approuvé est bien celui utilisé) | à répondre | CORRIGÉ |
| X4/C4(classifier) | Codex+Cubic | P1 | ingestion_agents/classifier.py:59 / quality_agent.py:97 | niveau/voie/programme_conformity toujours `True`, jamais vérifiés, mais utilisés dans la décision de routage | Confirmé : hardcodés `True`, jamais reliés à `rejection_reasons` avant ce lot | Corrigé (option 2 du mandat : ROUTED bloqué tant qu'aucune vérification réelle n'existe) | Les trois champs valent désormais `False` ("non vérifié") ; `QualityAgent` les traite comme motifs de rejet explicites (`*_not_verified`) — `ROUTED` devient structurellement inatteignable tant qu'aucun classifieur réel ne les remplace | `tests/test_lot44d_classifier.py` (mis à jour, changement intentionnel documenté) ; `tests/integration/test_lot44e_worker_e2e.py::test_full_chain_reaches_quality_checked_with_consistent_job_id` (renommé depuis "reaches_routed", vérifie l'arrêt à QUALITY_CHECKED avec les 3 motifs) | à répondre | CORRIGÉ (changement de comportement intentionnel) |
| X1 | Codex | P1 | ingestion_worker/cli.py:112 | `reap_expired_job_leases` jamais appelée par du code non-test | Confirmé | Corrigé | `_reap_expired_leases()` (jobs + resources) appelée à chaque itération de la boucle `main()`, avant tout `claim_job` | `tests/integration/test_lot44e_jobs_control.py::TestWorkerCliReapsExpiredLeasesOnEachIteration` — job réellement bloqué par un conteneur "crashé", reap réel, re-réclamation réelle prouvée | à répondre | CORRIGÉ |
| X5/C22 | Codex+Cubic | P2 | create_job_cli.py:146 / scout.py:62 | dedup_key par défaut basé sur `source_url` (CLI) mais Scout hache `canonical_url` — identités divergentes en cas de redirection | Confirmé | Corrigé | `_default_dedup_key` hache désormais `canonical_url` (même identité que Scout) | Régression couverte par `TestFindOrCreateJob` (C16) — identité canonique désormais cohérente bout en bout | à répondre | CORRIGÉ |
| C23 | Cubic | P1 | infra/docker-compose.v2.yml:11 | `rag_ingestion_artifacts_data` absent du plan de sauvegarde | Confirmé | Corrigé | Ajouté à `backup-volumes.sh` (avec vérification d'existence — le volume est opt-in, peut ne jamais exister) ; `restore-volumes.sh` déjà générique, aucun changement requis | `backup-volumes.sh` relu manuellement ; vérification d'existence testée conceptuellement (volume absent → skip gracieux, pas d'échec) | à répondre | CORRIGÉ |
| C24 | Cubic | P1 | ingestion_control/claim.py:111 | `lease_duration_s <= 0` crée un bail déjà expiré | Confirmé | Corrigé | Validation `> 0` ajoutée dans `claim_resource` **et** `claim_job` (même classe de bug, corrigé par symétrie) | `tests/integration/test_lot44e_jobs_control.py::test_rejects_non_positive_lease_duration` | à répondre | CORRIGÉ |
| C25 | Cubic | P1 | ingestion_agents/extractor.py:77 | Contenu relu jamais vérifié contre `size_bytes`/`sha256` | Confirmé | Corrigé | `ArtifactIntegrityError` levée avant extraction si taille ou SHA-256 ne correspondent pas | `tests/test_lot44d_extractor.py` : 2 nouveaux tests (taille incohérente, hash incohérent) — tests existants corrigés pour utiliser des empreintes réelles (ils utilisaient des valeurs factices non cohérentes, un vrai bug de fixture révélé par ce correctif) | à répondre | CORRIGÉ |
| C26 | Cubic | P2 | ingestion_agents/planner.py:44 | `max_queries_per_run` jamais appliqué | Confirmé | **Corrigé (revue incrémentale, passe 2)** — reclassé : reporté à tort en première passe comme hors périmètre ; correction simple, isolable, et un profil gouverné porte ce budget explicitement, le laisser sans effet était inacceptable pour une release gouvernée | `ordered_queries = ordered_queries[:profile.max_queries_per_run]`, appliqué après déduplication et après assemblage gap-targets-d'abord — les cibles de couverture prioritaires ne sont jamais évincées par un sujet générique du profil sous contrainte de budget | `tests/test_lot44d_planner.py::TestPlanSearchCoreMaxQueriesPerRunBudget` : 6 nouveaux tests (budget=1, budget < nombre de gap-targets, budget > total disponible, déduplication avant troncature, priorité gap-targets préservée, jamais de dépassement) — 4 des 6 vérifiés en échouant sur le code non corrigé avant validation | à répondre | CORRIGÉ |
| C27/N | Cubic | P2 | ingestion_worker/cli.py:55 | `--poll-interval-s` négatif/infini accepté | Confirmé | Corrigé | Validateur `_finite_non_negative_float` à l'analyse des arguments | `tests/test_lot44f_worker_cli_argparse.py` : 6 tests | à répondre | CORRIGÉ |
| C28 | Cubic | P2 | infra/docker-compose.v2.yml:180 | `.env.example` ne documente pas les nouvelles variables requises | Confirmé — mais résolu structurellement, pas documentairement | Corrigé (par construction) | Les nouvelles variables ne sont référencées **que** dans `docker-compose.ingestion.yml` (opt-in) — `.env.example`/`make v2-up` normal n'en ont jamais besoin ; le déploiement normal n'est plus cassé (c'était le vrai problème, pas l'absence de documentation) | `tests/test_prod_compose_config_mount.py::test_v2_compose_alone_never_requires_ingestion_env_vars` | à répondre | CORRIGÉ (avec C4) |
| C29 | Cubic | P2 | migrations/001...sql:186 | Commentaire décrit un `ON CONFLICT`/`attach_candidate` qui n'existe pas | Confirmé : primitive Python inexistante référencée par erreur | Corrigé | Commentaire réécrit pour décrire la réalité actuelle (dédup au niveau job via `find_or_create_job`, `create_resource` en filet de sécurité sans récupération silencieuse) | — (commentaire uniquement) | à répondre | CORRIGÉ |
| C30 | Cubic | P2 | ingestion_worker/runner.py:352 | Le worker réclame n'importe quel `job_type` | Confirmé | Corrigé | `claim_job` accepte un filtre `job_types` optionnel ; `runner.py` passe `SUPPORTED_JOB_TYPES = ("resource_pipeline",)` | `tests/integration/test_lot44e_jobs_control.py` : 3 nouveaux tests (job d'un autre type jamais réclamé, type correspondant toujours réclamé, tuple vide rejeté) | à répondre | CORRIGÉ |
| C31 | Cubic | P2 | ingestion_control/transitions.py:105 | CAS n'exige pas que `run_id` corresponde à la ressource | Confirmé | Corrigé | `AND run_id = %s` ajouté au `WHERE` du CAS | Couverture transitive via toute la suite `test_lot44d_transitions.py`/intégration (tous les appelants passent déjà le bon `run_id`) — aucune régression | à répondre | CORRIGÉ |
| C32/G | Cubic | P2 | ingestion_agents/fetcher.py:72 | Licence jamais persistée dans l'`ArtifactRecord` durable | Confirmé : reconstruite après `persist_artifact` sur une copie mémoire jamais écrite | Corrigé | `license` propagée dans `run_fetcher`/`build_artifact_core` **avant** persistance ; copie en mémoire supprimée de `runner.py` | `tests/test_lot44d_fetcher.py::test_license_is_persisted_on_the_returned_artifact` | à répondre | CORRIGÉ |
| C33 | Cubic | P2 | ingestion_worker/storage.py:34 | `read_artifact` ne vérifie aucune frontière de répertoire | Confirmé | Corrigé | `ArtifactPathEscapeError` si le chemin résolu (symlinks compris) sort de `base_dir` résolu | `tests/test_lot44e_storage.py` (nouveau fichier) : 6 tests (traversée relative, chemin absolu, symlink, cas légitimes) | à répondre | CORRIGÉ |
| C34 | Cubic | P2 | create_job_cli.py:85 | `--dedup-key` opérateur non validé comme SHA-256 | Confirmé | Corrigé | Validation regex stricte avant toute écriture, échec explicite sinon | Vérifié manuellement (`_SHA256_HEX_PATTERN`) ; comportement couvert indirectement par la suite `TestFindOrCreateJob` qui exerce le format attendu | à répondre | CORRIGÉ |
| X4b/C35 | Cubic | P2 | ingestion_agents/quality_agent.py:97 | `reject_unknown_rights=False` toujours ignoré, rejet inconditionnel | Confirmé | Corrigé | Rejet conditionné à `profile.reject_unknown_rights` | `tests/test_lot44d_quality_agent.py` : 2 nouveaux tests | à répondre | CORRIGÉ |
| C36 | Cubic | P2 | ingestion_profiles/manifest.py:107 | Clés YAML dupliquées silencieusement résolues à la dernière valeur | Confirmé | Corrigé | `_DuplicateKeyRejectingLoader` (sous-classe de `SafeLoader`) rejette toute clé dupliquée dans n'importe quel mapping du document | `tests/test_lot44c_profile_manifest.py::TestManifestRejectsDuplicateYAMLKeys` : 2 nouveaux tests (top-level, imbriqué dans une entrée) | à répondre | CORRIGÉ |
| C37 | Cubic | P2 | infra/scripts/bootstrap_ingestion_control_schema.sh:66 | Fichier `.sql` mal nommé silencieusement ignoré par le glob de découverte | Confirmé : le glob de découverte ne trouvait que les fichiers déjà bien nommés, la vérification stricte ne s'appliquait donc jamais aux fichiers réellement malformés | Corrigé | `find -name '*.sql'` (large) au lieu du motif déjà strict — la vérification stricte existante s'applique désormais à tout `.sql` présent | Comportement inchangé sur le cas nominal (rejeu complet du rollback rehearsal vert) ; cas malformé non testé isolément (script shell, hors harnais pytest) — risque résiduel documenté | à répondre | CORRIGÉ (test manuel, pas de harnais automatisé) |
| C38 | Cubic | P2 | ingestion_worker/cli.py:54 | `--max-iterations` négatif accepté silencieusement | Confirmé | Corrigé | Validateur `_positive_int` à l'analyse des arguments | `tests/test_lot44f_worker_cli_argparse.py` | à répondre | CORRIGÉ |
| C39 | Cubic | P2 | ingestion_profiles/registry.py:137 | `profile_version` avec saut de ligne final contourne la validation | Confirmé empiriquement (`.match()` sans `MULTILINE` tolère un `\n` final avant `$`) | Corrigé (suggestion Cubic appliquée telle quelle) | `PROFILE_VERSION_PATTERN.fullmatch(...)` remplace `.match(...)` | `tests/test_lot44c_profile_registry.py::test_version_with_trailing_newline_is_rejected` | à répondre | CORRIGÉ |
| C40 | Cubic | P2 | migrations/004...sql:97 | `ADD CONSTRAINT` non idempotent — rejeu échoue | Confirmé (PostgreSQL n'a pas de `ADD CONSTRAINT IF NOT EXISTS`) | Corrigé | Bloc `DO $$` avec vérification `pg_constraint` avant `ADD CONSTRAINT` | `tests/integration/test_lot44f_migration_rollback_rehearsal.py` rejoué (rollback+réapplication complets, y compris 004) | à répondre | CORRIGÉ |

## Signalements Codex (5 — recoupent Cubic, référencés ci-dessus)

| ID | Ligne | Recoupe | Statut |
|---|---|---|---|
| X1 | ingestion_worker/cli.py:112 | — (unique à Codex) | CORRIGÉ |
| X2 | ingestion_worker/runner.py:182 | C21/startup_gate | CORRIGÉ |
| X3 | ingestion_agents/scout.py:56 | C3 | CORRIGÉ |
| X4 | ingestion_agents/classifier.py:59 | C4(classifier) | CORRIGÉ |
| X5 | create_job_cli.py:146 | C22 | CORRIGÉ |

## GitGuardian (5 occurrences, 1 seul incident logique)

**Toutes les 5 occurrences relèvent du même incident GitGuardian (`35834636`)** : détection générique "Generic Password" sur des chaînes littérales `PG_SUPERUSER_PASSWORD = "test-password"`, `APP_PASSWORD = "ingestion-control-app-test-pw"`, `MIGRATOR_PASSWORD = "ingestion-control-migrator-test-pw"` dans 5 fichiers de test d'intégration (`test_lot44e_jobs_control.py`, `test_lot44d_chain_wiring.py`, `test_lot44c_profile_validation_events.py`, `test_lot44f_worker_resume.py`, `test_lot44e_worker_e2e.py`).

**Triage (valeurs non affichées ci-dessus conformément au mandat)** :
1. **Nature** : valeurs de test explicitement factices, jamais un identifiant réutilisable ni un secret réel.
2. **Portée** : chacune n'existe que le temps d'un conteneur PostgreSQL Docker jetable, créé (`docker run --rm`) et détruit (`docker rm -f`) dans le même processus de test, sur un réseau local (`127.0.0.1`, port éphémère). Aucune de ces valeurs n'a jamais existé en dehors du cycle de vie d'un test.
3. **Aucune correspondance avec un secret de production** : confirmé — ces chaînes n'apparaissent dans aucun fichier `.env`, aucun secret manager, aucune configuration de déploiement réel ; elles étaient générées/écrites uniquement par le code de test lui-même.
4. **Décision finale** : plutôt que de s'arrêter à la documentation (option initialement retenue), les 3 constantes littérales (`PG_SUPERUSER_PASSWORD`, `APP_PASSWORD`, `MIGRATOR_PASSWORD`) ont été remplacées par une génération dynamique (`secrets.token_urlsafe(24)`, calculée une fois par import de module) dans les 8 fichiers de test concernés (les 5 originaux + 3 nouveaux fichiers de cette remédiation qui reprenaient le même motif) — option également sanctionnée par le mandat ("la générer dynamiquement pour le test"), et qui a l'avantage de faire disparaître le signalement à la racine (plus de motif statique à détecter) plutôt que de compter sur un examen humain de la documentation à chaque scan futur. Aucune exclusion GitGuardian globale ni ciblée créée — devenue inutile.
5. Suite d'intégration complète revérifiée après ce changement (conteneurs PostgreSQL réels provisionnés avec les mots de passe dynamiques) : verte.

**Statut** : Résolu à la racine — plus de valeur statique à détecter dans le code source.

## Récapitulatif des tests ajoutés

- Unitaires : ~45 nouveaux cas répartis sur `test_lot44d_scout.py`, `test_lot44d_fetcher.py`, `test_lot44d_extractor.py`, `test_lot44d_classifier.py`, `test_lot44d_quality_agent.py`, `test_lot44c_profile_registry.py`, `test_lot44c_profile_manifest.py`, `test_ssrf_guard.py`, `test_prod_compose_config_mount.py`, `test_lot44e_storage.py` (nouveau), `test_lot44f_worker_cli_argparse.py` (nouveau).
- Intégration (PostgreSQL réel) : ~20 nouveaux cas répartis sur `test_lot44e_jobs_control.py`, `test_lot44e_worker_e2e.py`, `test_lot44f_role_provisioning_hardening.py` (nouveau), `test_lot44f_migration_upgrade_paths.py` (nouveau) — dont un test de concurrence réel (2 threads, 2 connexions PostgreSQL) et un test d'inspection de processus réel (`ps -eo args`).
- Un test existant renommé/réécrit intentionnellement (`test_full_chain_reaches_routed...` → `test_full_chain_reaches_quality_checked...`) suite au changement de comportement volontaire de C4(classifier)/X4 (ROUTED désormais inatteignable sans classifieur réel — référence corrigée en revue incrémentale, passe 2 : ce paragraphe désignait auparavant à tort un « item L » qui ne correspond à aucune ligne de ce document).

## Validation finale

Voir `docs/reports/lot_44f_reconciliation_worker_isole.md` (section mise à jour) pour les commandes exactes et codes de sortie : `ruff check .`, `mypy src`, suite unitaire complète, suite d'intégration complète, tous verts sur le HEAD remédié.

## État des checks GitHub sur le HEAD remédié (`735945fc2f8dc67beef6c8d3c0a477f868d51dbf`)

| Check | Résultat | Commentaire |
|---|---|---|
| `services/rag-engine` | ✅ pass (7m46s) | CI complète (lint/typecheck/tests) |
| `services/rag-pedago` | ✅ pass (5m35s) | |
| `services/cockpit` | ✅ pass | |
| `governance locks guard` | ✅ pass | Aucun verrou de gouvernance modifié |
| `packages/contracts` | ✅ pass | Contrat + golden queries |
| `repository controls` | ✅ pass | |
| `Evaluate trusted human review` | ❌ fail (attendu) | ADR-0025 : exige une review `APPROVED` du Code Owner `@abenrhouma` sur ce HEAD exact — n'existe pas encore, comportement fail-closed correct, pas un défaut à corriger |
| `GitGuardian Security Checks` | ❌ fail | Le code source actuel ne contient plus aucune valeur statique flaguée (remplacées par génération dynamique, cf. section GitGuardian ci-dessus) — mais GitGuardian scanne l'historique complet des commits de la PR, et le commit initial `9306f1c` contenait encore ces chaînes dans son diff. Réécrire l'historique (rebase/force-push) est explicitement interdit par le mandat. Un dismissal manuel de l'incident sur le dashboard GitGuardian par un humain habilité reste la seule voie restante pour faire passer ce check au vert — hors de la portée d'un agent de codage. |
| `cubic · AI code reviewer` | ⚪ neutral/skipped | Quota mensuel de révision de l'équipe atteint (82 062/80 000 lignes, reprise le 2026-09-01) — contrainte externe de facturation, sans rapport avec le contenu de cette PR. Le message de cubic confirme lui-même que « votre équipe a corrigé 614 problèmes signalés par cubic jusqu'ici » — cohérent avec les 44 fils résolus par cette remédiation. |

---

# Revue incrémentale — passe 2 (28 nouveaux signalements Cubic)

Une revue Cubic incrémentale, publiée après la fin de la passe 1
ci-dessus, a ajouté 28 nouveaux problèmes (11 P1, 10 P2, 7 P3) sur le HEAD
alors atteint. Cette section documente leur remédiation, item par item,
selon la même règle que la passe 1 : diagnostic réel avant correction,
test de non-régression réel ajouté et vérifié — dont, pour chaque
correctif de cette passe, une vérification supplémentaire que le test
échoue bien sur le code non corrigé (revert temporaire, exécution,
restauration) avant d'être accepté comme preuve.

**HEAD au moment de cette revue incrémentale** : `9898c045236669a2aae1a2b533afe9e47f709eac` (déclaré NON approuvé par l'opérateur au moment où cette seconde revue a été communiquée — cf. mandat).
**HEAD remédié (fin de cette passe)** : voir le rapport de lot final pour le SHA exact après commit/push de cette passe.

## P1 (11) — items lettrés A-I par le mandat opérateur, plus deux items P1 supplémentaires

| Item | ID Cubic | Fichier | Problème | Correction | Tests |
|---|---|---|---|---|---|
| A | 3725928985 | migrations/004_jobs.sql | Garde d'idempotence de la FK `workflow_events_job_id_fkey` ne filtrait que par `conname`+`connamespace` — une contrainte homonyme sur une **autre** table du schéma aurait fait passer la garde comme "déjà présent", sautant silencieusement la création de la vraie FK | Scope explicite par `conrelid = 'ingestion_control.workflow_events'::regclass` + vérification que la définition trouvée (`pg_get_constraintdef`) correspond exactement à la FK attendue, sinon `RAISE EXCEPTION MIGRATION_004_FK_MISMATCH` | `test_lot44f_migration_upgrade_paths.py::TestMigration004FKConstraintScoping` — 2 tests (contrainte homonyme sur une autre table ; contrainte de même nom mais définition différente sur la bonne table), tous deux contre PostgreSQL réel |
| B | 3725928989 | migrations/005_jobs_status_reconcile.sql | Backfill `claimed`→`running` inconditionnel — une ligne `claimed` sans bail actif (absent ou déjà expiré) deviendrait `running` mais plus jamais réclamable (`claim_job` ne sélectionne que `queued`) | Politique déterministe à deux étapes : bail actif (`lease_token` non nul ET `lease_expires_at` réellement futur) → `running` ; sinon → `queued` avec `claimed_by`/`lease_token`/`lease_expires_at` explicitement nettoyés | `TestMigration005ClaimedStatusBackfill` — 3 tests (bail actif, bail expiré, aucun bail), PostgreSQL réel |
| C | — (mandat opérateur) | infra/scripts/provision_ingestion_control_roles.sh | Seule la collision `MIGRATOR_ROLE == APP_ROLE` était rejetée — `MIGRATOR_ROLE == PGUSER` ou `APP_ROLE == PGUSER` retirerait ses propres privilèges au compte administratif utilisé pour se connecter, en plein script | Deux vérifications supplémentaires ajoutées, avant toute écriture | `test_lot44f_role_provisioning_hardening.py::TestRoleCollisionRejected` — 2 nouveaux tests, vérifient aussi qu'aucune altération du rôle PGUSER n'a lieu quand le script est rejeté |
| D | — (mandat opérateur) | infra/scripts/provision_ingestion_control_roles.sh | Un rôle préexistant rendu membre d'un autre rôle (dérive manuelle) conservait cette appartenance après provisioning — `NOINHERIT` empêche l'héritage automatique de privilèges mais jamais un `SET ROLE` explicite vers ce rôle | `REVOKE` inconditionnel de toute appartenance trouvée sur `MIGRATOR_ROLE`/`APP_ROLE` (aucune appartenance n'est jamais accordée par ce script — toute appartenance trouvée est par construction non autorisée) | `TestPreExistingMisconfiguredRoles::test_preexisting_role_membership_is_revoked_and_set_role_refused` — rôle rendu membre d'un rôle `SUPERUSER` tiers, provisioning rejoué, `SET ROLE` réel tenté et refusé (`InsufficientPrivilege`) |
| E | — (mandat opérateur) | rollbacks/*.down.sql (runner absent) | `psql -f rollback.sql` sans `--single-transaction` relâche un verrou entre deux instructions — fenêtre de course pour une écriture concurrente entre `LOCK TABLE` et la garde/le DROP | Nouveau runner officiel `infra/scripts/rollback_ingestion_control_schema.sh` : quiescence de l'ingestion (job `running`/bail actif → refus), transaction unique pour toute la plage, verrouillage des six tables en ordre alphabétique fixe dès le début, mise à jour de `schema_migrations` dans la même transaction, commit uniquement si tout réussit | `test_lot44f_rollback_runner.py` (nouveau, 7 tests) — dont un test de blocage concurrent réel (verrou antagoniste + file d'attente équitable PostgreSQL, sans dépendre d'un minutage) |
| F | — (mandat opérateur) | migrations/006_resource_candidates_and_artifacts_payload.sql | Préflight de vacuité sans aucun verrou — une transaction concurrente pouvait valider un INSERT entre le préflight et l'`ALTER TABLE`, recevant le DEFAULT inutilisable sans être détectée | `LOCK TABLE ... ACCESS EXCLUSIVE` ajouté avant le préflight, retenu jusqu'à la fin de la migration | `TestMigration006BlocksOnNonEmptyTable` — test structurel (ordre textuel `LOCK TABLE` avant `DO $nexus$`) + test comportemental de blocage réel |
| G | 3725929008 | ingestion_control/jobs.py, retry.py | `complete_job`/`record_job_retry`/`set_job_resource_id`/`record_retry` utilisaient `now()` (figé au début de transaction) au lieu de `clock_timestamp()` (horloge murale réelle) pour la comparaison d'expiration de bail | `clock_timestamp()` partout | 4 tests transactionnels réels (transaction commencée avant expiration, opération tentée après expiration réelle dans la même transaction) — chacun vérifié en échouant sur `now()` avant validation |
| H | — (mandat opérateur) | Makefile (`v2-ingestion-up`) | `up -d` sans `--wait` : succès rapporté dès que les conteneurs sont *créés*, indépendamment de l'issue réelle du migrateur ou de la santé du worker | `--wait` ajouté | `test_lot44f_ingestion_up_failure.py` (nouveau, 3 tests réels avec Docker/Compose réel) — migrateur en échec détecté, migrateur en succès accepté, worker jamais healthy détecté même quand le migrateur réussit |
| I | — (mandat opérateur) | ingestion_worker/cli.py (attestation absente) | Aucune vérification que la connexion PostgreSQL réelle du worker corresponde au rôle attendu et à une politique de moindre privilège | Nouveau module `ingestion_control/attestation.py` : `current_user` attendu, non-superutilisateur, sans CREATEDB/CREATEROLE/REPLICATION/BYPASSRLS, sans propriété du schéma, sans privilège excédentaire sur `workflow_events`, sans appartenance à un autre rôle — appelé avant toute autre opération, refuse de démarrer sinon | `test_lot44f_worker_attestation.py` (nouveau, 6 tests réels) — DSN superutilisateur explicitement refusé |
| P1 | 3725929007 | create_job_cli.py, ingestion_control/jobs.py | `dedup_key` seul (sans collection) comme identité d'idempotence job-level — même URL dans deux collections traitée à tort comme un doublon | `find_active_job_by_dedup_key`/`find_or_create_job` filtrent désormais par `(collection, dedup_key)` (jointure `ingestion_runs`), jamais `dedup_key` seul | `test_lot44e_jobs_control.py::test_same_dedup_key_in_two_different_collections_creates_two_jobs` + `test_lot44f_create_job_cli_idempotency.py` (bout-en-bout via le CLI réel) |

## P2 (10)

| ID Cubic | Fichier | Problème | Correction | Tests |
|---|---|---|---|---|
| 3725929016 | infra/scripts/bootstrap_ingestion_control_schema.sh | La création initiale du schéma/registre (`registry_schema_sql`) s'exécutait hors de toute protection — `CREATE SCHEMA/TABLE IF NOT EXISTS` n'est pas atomique contre une création concurrente en READ COMMITTED ; de plus, `apply_migration` insérait dans `schema_migrations` sans `ON CONFLICT`, faisant échouer un second exécutant bloqué puis débloqué sur une version déjà commise par le premier | Verrou advisory transactionnel ajouté autour de la création initiale (même clé qu'`apply_migration`) ; `INSERT ... ON CONFLICT (version) DO NOTHING` sur l'enregistrement de version | `TestConcurrentBootstrapInvocations` (nouveau) — deux instances réelles du script lancées en parallèle contre un volume neuf, les deux doivent réussir |
| 3725929018 | rollbacks/*.down.sql | Ordre de verrouillage non canonique entre rollbacks — risque d'interblocage avec un processus concurrent verrouillant plusieurs tables dans un ordre différent | **Résolu par item E** : le runner officiel verrouille désormais les six tables dans un ordre alphabétique fixe, dès le début de la transaction unique, indépendamment de la plage réellement annulée | Couvert par `test_lot44f_rollback_runner.py` (item E) |
| 3725929020 | ingestion_agents/scout.py | Seuls les hôtes dérivés d'URL (`_hostname_of`) étaient normalisés (casse, point final) — `domain` déclaré par l'appelant et les entrées `allowed_domains` du profil étaient comparés tels quels, pouvant rejeter à tort un hôte légitime saisi avec une casse différente | Normalisation identique (`_normalize_hostname`) appliquée aux trois sources avant toute comparaison | `test_lot44d_scout.py` — 2 nouveaux tests (domaine déclaré en casse mixte + point final, entrée `allowed_domains` en casse mixte), vérifiés en échouant sur le code non corrigé |
| 3725929025 | ingestion_agents/fetcher.py | `_NON_RETRYABLE_STATUS_CODES` énumérait les 4xx non-réessayables — tout code 4xx absent de cette liste (402, 406, 408, 409, 411-428, 431...) était classé réessayable par défaut, à l'inverse de la politique voulue ("tous les 4xx sauf 429 sont définitifs") | Classification dérivée de la plage HTTP (4xx vs 5xx) plutôt que d'une énumération à maintenir manuellement — seul 429 est excepté | `test_lot44d_fetcher.py` — 17 codes de statut supplémentaires ajoutés au test paramétré existant |
| 3725929028 | create_job_cli.py | `create_ingestion_run` insérait toujours une nouvelle ligne `ingestion_runs` avant de savoir si un job actif existait déjà pour ce `dedup_key` — une soumission dupliquée laissait cette ligne orpheline, bloquée en `'planned'` pour toujours | Ligne marquée `'cancelled'` (jamais supprimée : le rôle applicatif ne détient pas `DELETE` sur `ingestion_runs`, privilège volontairement absent) quand `find_or_create_job` indique `created=False` | `test_lot44f_create_job_cli_idempotency.py::test_duplicate_submission_leaves_no_orphaned_planned_run` |
| 3725929033 | ingestion_worker/storage.py | `Path.resolve()` (vérification) puis `read_bytes()` (lecture) séparés — fenêtre TOCTOU où un composant du chemin peut être remplacé par un lien symbolique entre les deux appels | Ouverture atomique composant par composant (`os.open(..., dir_fd=..., O_NOFOLLOW)`) — vérification et lecture ne font plus qu'un seul appel système par composant, y compris les composants intermédiaires | `test_lot44e_storage.py::test_symlink_swap_race_between_validation_and_read_never_leaks_secret` — course réelle (deux threads, bascule symlink/fichier en boucle), le secret hors `base_dir` a été réellement intercepté par ce test sur l'implémentation non corrigée avant validation |
| 3725929035 | ingestion_profiles/manifest.py | Une clé YAML non-hachable (séquence/mapping utilisé comme clé) faisait échouer le contrôle de doublons du chargeur personnalisé avec un `TypeError` brut, jamais capturé par `except yaml.YAMLError` | `TypeError` capturé et re-levé comme `yaml.constructor.ConstructorError` (même hiérarchie que le reste des erreurs de ce chargeur) | `test_lot44c_profile_manifest.py::TestManifestRejectsUnhashableKeys` — 2 tests |
| 3725929039 | ingestion_profiles/manifest.py | Aucune politique explicite pour les ancres/alias/clés de fusion YAML (`&ancre`, `*alias`, `<<`) | Rejet inconditionnel — un manifest de production doit rester une déclaration littérale, auditable par simple lecture humaine, jamais reconstituée indirectement | `test_lot44c_profile_manifest.py::TestManifestRejectsAnchorsAndMergeKeys` — 3 tests (ancre, alias, clé de fusion) |
| 3725929042 | infra/scripts/backup-volumes.sh | Noms de volumes hardcodés supposés "nus" — `docker compose up` sans `-p`/`COMPOSE_PROJECT_NAME` explicite préfixe systématiquement chaque volume du nom du projet ; le script échouait alors silencieusement à trouver le volume réel ("Skipping... does not exist") | Résolution par étiquettes Compose (`com.docker.compose.project`+`com.docker.compose.volume`) — jamais un filtrage par sous-chaîne de nom (constaté dangereux empiriquement : plusieurs projets sans rapport sur la même machine partagent des volumes de même clé Compose sous des noms réels différents) | `infra/scripts/tests/test_backup_volumes_project_prefix.sh` (nouveau) — volume réel + volume "decoy" d'un autre projet avec la même clé, vérifie que seul le bon est sauvegardé |
| — | (absorbé par P1 3725929007) | | | |

## P3 (7)

| ID Cubic | Fichier | Problème | Correction | Tests |
|---|---|---|---|---|
| 3725929045 | test_lot44f_worker_resume.py | Fixture `tmp_path` déclarée mais jamais utilisée dans `test_second_claim_finds_no_job_while_first_holds_the_lease` | Retirée | — (retrait de code mort, revérifié par la ré-exécution du test) |
| 3725929049 | infra/scripts/bootstrap_ingestion_control_schema.sh | Le commentaire annonçait explicitement la découverte d'un fichier `.SQL` (majuscules) mais `find -name` reste sensible à la casse sur Linux — `-iname` seul le garantit | `-iname '*.sql'` | Vérifié empiriquement (`find -name` vs `-iname` sur un fichier `.SQL` de test) ; suite de rollback rehearsal rejouée sans régression |
| 3725929051 | test_lot44f_migration_upgrade_paths.py | Migration 006 porte deux préflights de vacuité indépendants (`resource_candidates` ET `artifacts`) — seul le premier était testé | Nouveau test isolant spécifiquement le second (`artifacts` non vide, `resource_candidates` vide) | `test_migration_006_fails_explicitly_on_preexisting_artifact_row` |
| 3725929053 | ingestion_control/__init__.py | `ResourceLeaseConflictError` non réexportée au niveau du paquet, contrairement à `claim_resource` (qui la lève) déjà réexportée — incohérence pour un appelant externe | Ajoutée à l'import et à `__all__` | `test_ingestion_control_public_api.py` (nouveau) — vérifie aussi que tous les noms de `__all__` sont réellement importables |
| 3725929055 | test_lot44f_role_provisioning_hardening.py | Le test de surveillance `ps -eo args` pouvait passer de façon vacueuse si le thread d'observation n'avait, par minutage défavorable, jamais capturé le moindre processus psql | Assertion ajoutée : au moins un instantané doit contenir `psql` ET `--single-transaction` (flag caractéristique de ce script précis) | Le test lui-même, renforcé |
| 3725929056 | pr90_review_remediation_matrix.md | Référence à un « item L » ne correspondant à aucune ligne du document | Corrigée pour référencer explicitement C4(classifier)/X4 | — (ce document) |
| 3725929059 | docker-compose.ingestion.yml (commentaire) | Runbook `docs/runbooks/ingestion_control_go_live.md` référencé mais jamais créé | Créé — prérequis, démarrage, création de job, rollback, attestation, sauvegarde/restauration, diagnostics courants | — (documentation) |

## Item supplémentaire (hors liste Cubic, mandat opérateur explicite)

| Item | Fichier | Problème | Correction | Tests |
|---|---|---|---|---|
| planner.py (reclassement de C26) | ingestion_agents/planner.py | `max_queries_per_run` jamais appliqué — reporté à tort comme hors périmètre en passe 1 | `ordered_queries` borné après déduplication et assemblage gap-targets-d'abord | `test_lot44d_planner.py::TestPlanSearchCoreMaxQueriesPerRunBudget` — 6 tests, cf. ligne C26 mise à jour ci-dessus |

## Validation de cette passe

- `ruff check src/ingestor/` : aucune erreur.
- `mypy` sur les 11 fichiers source modifiés dans cette passe : aucune erreur.
- Suite unitaire complète (`pytest -m "not integration"`) : 1730 tests, tous verts.
- Suite d'intégration complète (`pytest tests/integration/`, PostgreSQL/Docker réels) : 117 passés, 9 ignorés (hors périmètre de cette passe, pré-existants), 0 échec.
- Chaque correctif de cette passe a été vérifié individuellement en échouant sur une version temporairement non corrigée du code (revert ciblé, exécution, restauration) avant d'être accepté — pas seulement un test qui passe sur le code déjà corrigé.
