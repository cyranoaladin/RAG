[pytest]
testpaths = tests
norecursedirs =
    services
    packages
    corpus
    docs
    .git
    __pycache__
    node_modules
    .venv
addopts = -q -m "not network and not e2e"
markers =
    network: tests requiring a network or database service
    e2e: browser tests against an explicitly supplied environment
